# Build configuration. Project metadata still lives in setup.py; this file
# provides the modern build-system table so `python -m build` and PyPI tooling
# work (replacing the retired ez_setup bootstrap).
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
